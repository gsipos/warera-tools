---
description: 'TypeScript is the preferred language — use strict typing everywhere'
applyTo: '**/*.ts, **/*.tsx'
---

# TypeScript

- Use TypeScript for all new code
- TypeScript is the preferred language whenever possible
- Define explicit types for component props and function signatures
- Avoid `any` — use proper types or `unknown` when the type is genuinely not known
